{% extends 'base.html.twig' %}

{% block title %}Contains{% endblock %}

{% block body %}
    <h1>Contains</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contain.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ contain.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contains_index') }}">back to list</a>

    <a href="{{ path('app_contains_edit', {'id': contain.id}) }}">edit</a>

    {{ include('contains/_delete_form.html.twig') }}
{% endblock %}
