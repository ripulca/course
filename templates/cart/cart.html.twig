{% extends 'base.html.twig' %}

{% block title %}Cart{% endblock %}

{% block body %}
    <h1>Cart</h1>

    <div class="container">
        <div class="position-relative overflow-hidden p-3 p-md-5 text-center bg-light">
            <div class="row">
                {% for medicine in medicines %}
                        <div class="col-md-5 p-lg-2 mx-auto">
                            <a href="{{ path('app_medicine_show', {'id': medicine.medicine.id}) }}">
                                <div>{{medicine.medicine.name}}</div>
                                <div>{{medicine.medicine.price}}₽</div>
                                <div>{{medicine.amount}}</div>
                            </a>
                            {{ include('cart/_delete_form.html.twig') }}
                        </div>
                {% else %}
                    <p colspan="5">no records found, please, come back later</p>
                {% endfor %}
            </div> 
        </div>
    </div>

    <a href="{{ path('app_medicine_index') }}">back to list</a>

{% endblock %}